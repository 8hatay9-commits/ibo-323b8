[build]
  publish = "live"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."scheduled-scan"]
  schedule = "*/2 * * * *"
